spring:
  main:
    banner-mode: off # Don't show banner in tests

# Disable all scheduled jobs in tests by default
scheduled-tasks:
  maven-version-discovery:
    enabled: false
  minecraft-version-discovery:
    enabled: false
